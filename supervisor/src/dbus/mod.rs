//! NetworkManager, BlueZ, MPRIS, and Polkit D-Bus interfaces (build-steps.md §1). `polkit`
//! (Phase 5) and `network` (Phase 16) exist so far; BlueZ/MPRIS are later phases.

pub mod network;
pub mod polkit;
